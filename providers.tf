provider "google" {
  # ALM will automatically inject credentials via GOOGLE_OAUTH_ACCESS_TOKEN
}

provider "google-beta" {
  # ALM will automatically inject credentials via GOOGLE_OAUTH_ACCESS_TOKEN
}
